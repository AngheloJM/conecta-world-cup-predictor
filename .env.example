# Credenciales de PostgreSQL (Docker)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=conecta
DB_PORT=5432

# Cadena de conexión usada por el backend Rust (sqlx).
# Debe coincidir con las credenciales de arriba.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/conecta
